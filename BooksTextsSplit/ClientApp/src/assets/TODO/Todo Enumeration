crate Get for books pair context fetching - created, but it is needs to make common method to read cache/Db

it is needs to save selected bookId before calling QuickView - to return back to version selection

под кнопку select bookId добавить количество версий этой книги
под кнопку select version добавить количество предложений и всякого разного об этой версии

сделать новые ветки на github - front-react back-c# with additional subbranches - фигня

YES - заменить три кнопки одной - view

сделать функцию типа радиобаттона - говорить ей, какой блок включить, а дальше она сама

если так же группировать по языку, то можно использовать существующий класс для выдачи из гета

try to use ValueTask Either<T, Task<T>> (содержит либо готовый результат (и практически нулевой оверхед), либо полноценный аллоцированный Task, если нужно)


1. выбор радио без дополнительной кнопки, поставить умолчание в выбор языка и убрать локальный стейт
2. сделать третий блок страницы - собственно, загрузка файлов на сервер и отчёт
3. количество записей в базе в заголовке и кнопка показать/спрятать детали
4. добавить во второй блок параметры самой книги и редактируемое поле аннотации
5. к второму блоку добавить вывод инфы из базы по автору, названию, id книги - показать, сколько уже есть версий этой книги в базе
6. сделать описания и значения files/books отдельными колонками, выводить каждое в одном блоке flex/grid, сделать разные цвета для строк, чтобы была контрастная таблица, передавать значение количества строк в CSS, вынести функцию отдельно и потом в отдельный файл
    index: "File No: ",

сервер:
1. пользователи в базе, отдельная таблица или правильный метод от Майкрософт
2. написать интерфейс обёртки редиса и всем обращаться только через него
3. убрать в контроллере все обращения к данным в сервис 
4. получать все надписи с сервера для страницы загрузки (и остальных тоже - у каждой отдельный гет? или параметр у одного)

сделать первую страницу настроек и там список описаний файла и книги - что показывать, что нет

автоматический выбор языка - сначала на странице выбор включения (может, ещё в настройках), потом на сервере

добавить на сервер проверку наличия данных о пользователях, если их нет - не стартовать